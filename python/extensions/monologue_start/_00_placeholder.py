# Monologue Start Extension - Placeholder
# This file ensures the monologue_start directory is recognized by Agent Zero

from agent import Agent

# This extension is called at the start of each agent monologue
# You can add custom pre-processing here if needed

def execute(agent: Agent, **kwargs):
    """
    Called at the start of agent monologue

    Args:
        agent: The Agent instance
        **kwargs: Additional arguments
    """
    # Placeholder - no action needed for basic functionality
    pass
